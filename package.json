{
  "name": "dwebx-encoding",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "https://github.com/distributedweb/dwebx-encoding.git",
  "description": "Library for the encoding/decoding of dWeb buffers.",
  "dependencies": {
    "safe-buffer": "^5.0.1"
  },
  "devDependencies": {
    "standard": "^9.0.0",
    "tape": "^4.5.1"
  },
  "scripts": {
    "test": "tape test.js && standard"
  }
}
